<%= render "navbar"%>

<div class="container">
<h1 class="century uppercase" style="text-align:center">Artistas</h1>
  <div class="row" style="margin-top:20px">
    <div class="col-md-8 col-md-offset-2">
      <% @designers.each_with_index do |el,index| %>
          <%if index.even? %>
            <div class="col-md-6 thumbnail supercenter">
              <div class="col-sm-6 artists" style="margin:37.5px 0; background-image:url(<%=el.avatar.url(:thumb) %>);background-size:cover" >
              </div>
              <span class="divider"></span>
              <div class="col-sm-6">
                <div class="caption">
                  <h3 class="title century"><%=el.name%> <%=el.lastname%></h3>
                  <p class="century"><%=el.type_Of_Artist%></p>
                </div>
              </div>
            </div>
          <% else %>
            <div class="col-md-6 thumbnail supercenter" style="padding-left:30px">
              <div class="col-sm-6 artists" style="margin:37.5px 0; background-image:url(<%=el.avatar.url(:thumb) %>);background-size:cover" >
              </div>
              <span class="divider"></span>
              <div class="col-sm-6">
                <div class="caption">
                  <h3 class="title century"><%=el.name%> <%=el.lastname%></h3>
                  <p class="century"><%=el.type_Of_Artist%></p>
                </div>
              </div>
            </div>
          <% end %>
      <% end %>
    </div>
  </div>
</div>

<%= render "footer"%>